using System;
class ForbiddenApp
{
    public static void Main()
    {
        bool f = false, b;
        b = f;
        Console.WriteLine("f = " + f + ", b = " + b);
        // int i;
        // i = f; // 에러 : 묵시적 형변환 금지
        // i = (short)f; // 에러 : 명시적 형변환 금지
    }
}
